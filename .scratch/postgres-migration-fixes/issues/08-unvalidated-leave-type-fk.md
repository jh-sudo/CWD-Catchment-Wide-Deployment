Type: bug
Status: done

> *This was generated by AI during triage.*

Found during code review of the JSON→Postgres migration diff. User-supplied
`leaveType` is inserted directly into `leave_requests.leave_type`, a NOT
NULL foreign key referencing `leave_types.code`, with no validation
beforehand.

## Agent Brief

**Category:** bug
**Summary:** An invalid leaveType causes a generic 500 instead of a clean 400

**Current behavior:**
A client (or stale frontend build) sending a `leaveType` not present in
`leave_types` causes a foreign-key violation deep inside the insert
transaction, which surfaces as a generic 500 — a regression from the prior
JSON-backed version, which accepted any string `leaveType` unconstrained.

**Desired behavior:**
An invalid `leaveType` is rejected with a clear 400 response naming the
invalid value, whether caught by upfront validation or by translating the
underlying constraint violation.

**Key interfaces:**
- The leave-request creation path — needs to reject unknown `leaveType`
  values with 400, not let them reach the DB as an uncaught 500.

**Acceptance criteria:**
- [ ] Submitting a `leaveType` not present in `leave_types` returns 400 with
      a message identifying the invalid value
- [ ] Valid `leaveType` values continue to work unchanged

**Out of scope:**
- Changing what counts as a valid leave type

## Comments

> *This was generated by AI during triage.*

Resolved directly in this session, bundled with ticket 04 (same transaction,
same try/catch): the insert transaction in `leaveRequests.ts` is now wrapped
in a try/catch that maps a Postgres foreign-key violation (`23503`) on this
insert to a 400 naming the invalid `leaveType`. Verified via `pnpm typecheck`
(api-server package: clean).

Verified committed and present in code as of `d7fda06` (`leaveRequests.ts:350-352`).
