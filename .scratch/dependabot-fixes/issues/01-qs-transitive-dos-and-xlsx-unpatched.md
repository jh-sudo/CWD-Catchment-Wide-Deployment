Type: security
Status: done

> *This was generated by AI during triage.*

Found via GitHub's Dependabot alerts on the repo's default branch (`main`),
surfaced after pushing `phase-b/medium-features` (2026-09-08): "GitHub found
5 vulnerabilities on jh-sudo/CWD-Catchment-Wide-Deployment's default branch
(4 high, 1 moderate)."

## Agent Brief

**Category:** security (dependency vulnerabilities)
**Summary:** 5 open Dependabot alerts reduce to 2 distinct issues — a
transitive `qs` DoS (patched upstream, just needed forcing) and two `xlsx`
CVEs with no npm-published fix (SheetJS stopped publishing patched releases
to the npm registry after `0.18.5`).

**Alert breakdown** (via `gh api repos/.../dependabot/alerts`):

| Alert # | Package | Severity | Advisory | Patched on npm? |
|---|---|---|---|---|
| 50 | `qs` (transitive via `express`/`body-parser`) | moderate | [GHSA-x5fp-wj9c-mxmx](https://github.com/advisories/GHSA-x5fp-wj9c-mxmx) — array-limit bypass (DoS) | Yes, `6.16.0` |
| 7 / 3 | `xlsx` `0.18.5` | high | [GHSA-5pgg-2g8v-p4x9](https://github.com/advisories/GHSA-5pgg-2g8v-p4x9) — ReDoS | No |
| 6 / 2 | `xlsx` `0.18.5` | high | [GHSA-4r6h-8v6p-xvw6](https://github.com/advisories/GHSA-4r6h-8v6p-xvw6) — Prototype Pollution | No |

(`xlsx` appears twice each — once via the root `pnpm-lock.yaml`, once via
`artifacts/roster-dashboard/package.json`'s direct dependency on it — same
underlying package/version.)

`express@5.2.1` (latest as of this fix) still pins `qs@6.15.3` itself, so
the fix has to force the transitive resolution rather than wait for
upstream.

`xlsx` (SheetJS) is parsed client-side in `roster-dashboard` — the "brief"
upload flow (`UploadBrief.tsx`, `XLSX.read()` on a manager-dropped `.xlsx`
file) and roster-download `.xlsx` export (`XLSX.writeFile`). SheetJS
maintains active development but stopped publishing fixed builds to the npm
registry (published there caps at `0.18.5`); real fixes are distributed via
their own CDN or via community republishes to npm.

**Fix applied:**
- Root `package.json`: added `"pnpm": { "overrides": { "qs": "^6.16.0" } }`
  — forces every transitive consumer (express, body-parser) onto the
  patched `qs`, no code change needed.
- `artifacts/roster-dashboard/package.json`: swapped `"xlsx": "^0.18.5"` →
  `"@e965/xlsx": "^0.20.3"` — a community republish of current SheetJS
  source to npm (same API, own `.d.ts` types, proper ESM `exports` map),
  chosen over pointing at SheetJS's own CDN tarball to stay a normal
  registry dependency (no non-registry supply-chain source in the
  lockfile). `0.20.3` is past both vulnerable ranges (`<0.19.3` and
  `<0.20.2`).
- `artifacts/roster-dashboard/src/pages/UploadBrief.tsx`: only import site
  (`import * as XLSX from "xlsx"` → `"@e965/xlsx"`), with a comment
  explaining why.
- `pnpm install` to regenerate the lockfile — confirmed `qs@6.16.0` and
  `@e965/xlsx@0.20.3` resolved, no trace of the old `xlsx@0.18.5` package
  left in `pnpm-lock.yaml`.

**Branch note:** landed on a dedicated `fix/dependabot-qs-xlsx` branch off
`origin/main` (`8455c5a`) rather than `phase-b/medium-features`, since the
alerts track the default branch and `phase-b/medium-features` (34+ commits
of unrelated Replit-parity/roster work) wasn't ready to merge yet.

**Acceptance criteria:**
- [x] `qs` resolves to `>= 6.16.0` everywhere in `pnpm-lock.yaml`
- [x] `xlsx` (unpatchable) removed from the dependency tree entirely
- [x] `pnpm --filter roster-dashboard typecheck` clean
- [x] `pnpm --filter api-server typecheck` clean
- [ ] GitHub's 5 open Dependabot alerts close once this branch merges to
      `main` (not directly verifiable from here — confirm after merge)

## Comments

> *This was generated by AI during triage.*

Did not attempt a full `roster-dashboard` production build locally — the
repo's `vite.config.ts` requires several env vars (`PORT`, `BASE_PATH`, and
presumably more) not available in this session, unrelated to this change.
Verified instead via: `tsc` typecheck (both packages, clean), lockfile
inspection (correct resolved versions, old package fully gone), and
confirming `@e965/xlsx`'s published `exports` map carries a proper ESM
`import` condition (bundler-resolvable, same shape a Vite/Rollup build needs
that the plain `xlsx` package also provided via its `module` field).
Recommend a smoke-test of the Upload Brief / roster-download flows after
this deploys, since the swap wasn't exercised end-to-end.
