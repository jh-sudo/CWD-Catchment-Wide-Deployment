Type: bug
Status: done — the ordering-inconsistency part; the "missing WK4" claim didn't hold up

> *This was generated by AI during triage.*

## What happened

`PHActualPanel.tsx`'s local `UNIT_ORDER` array (inlined in the component) is missing **"WK4"**
entirely — `indexOf` returns -1, which sorts it to rank 999, i.e. dead last. It also groups units
by catchment-then-number (CP1-4, KG1-4, BU1-5, PJ1-4, WK1-3) rather than the canonical
round-robin-by-team-number order (CP1, KG1, BU1, PJ1, WK1, CP2, ...) used consistently elsewhere
in this repo's own backend (`phRoster.ts`'s `PH_DISPLAY_UNIT_SEQUENCE`).

Replit shares one canonical `lib/phUnitOrder.ts` (`PH_UNIT_ORDER`/`comparePHRows`) between its
backend and frontend instead of duplicating the ordering array locally; this repo doesn't have
that shared module, hence the local copy drifting out of sync and missing an entry.

## Fix

Port `lib/phUnitOrder.ts` as the single source of truth for PH unit ordering, and use it in
`PHActualPanel.tsx` instead of the local, incomplete `UNIT_ORDER` array. Verify it matches
`phRoster.ts`'s existing `PH_DISPLAY_UNIT_SEQUENCE` (or replace both with the shared module) so
frontend and backend can't drift apart again.

## Comments

- 2026-09-21: The "missing WK4" claim didn't survive verification — "WK4" doesn't appear
  anywhere else in this repo (no officer/unit data, no vehicle-arrangement defaults, no other
  `UNIT_ORDER` list), and `phRoster.ts` has no `PH_DISPLAY_UNIT_SEQUENCE` at all (that name
  doesn't exist in this codebase). This repo's actual unit set, cross-checked against
  `vehicleArrangement.ts`'s `HARDCODED_VEHICLE_DEFAULTS` and `rosterPlan.ts`'s cycle patterns, is
  20 units total (CP1-4, KG1-4, BU1-5, PJ1-4, WK1-3) — matching `PHHoliday.tsx`'s existing list
  exactly, which also has no WK4. Declined to add a unit code with no evidence it exists.
  - The real, verifiable bug: `PHActualPanel.tsx` and `PHHoliday.tsx` each kept their own copy of
    the unit order, and they used genuinely different schemes (catchment-then-number vs.
    interleaved-by-number) — a real inconsistency, just not the one originally described.
    Consolidated into a new shared `artifacts/roster-dashboard/src/lib/phUnitOrder.ts`
    (`PH_UNIT_ORDER`/`phUnitSortKey`), based on `PHHoliday.tsx`'s list (already explicitly
    commented as "canonical"). Both files now import from it instead of maintaining local copies
    that can silently drift apart.
