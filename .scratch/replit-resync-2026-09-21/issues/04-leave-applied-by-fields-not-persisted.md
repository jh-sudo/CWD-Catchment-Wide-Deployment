Type: bug
Status: not-started

> *This was generated by AI during triage.*

## What happened

`lib/db/src/schema/rosterLeaves.ts` already has `appliedBy`/`appliedByName`/`appliedAt` columns,
with a schema comment describing exactly this purpose ("who/when this leave entry was first
applied... preserved on every subsequent write"). But `POST /roster-plan/leave`
(`artifacts/api-server/src/routes/rosterPlan.ts`) never writes to them — it only writes
`appliedByName` into the activity-log entry, not onto the leave row itself. Replit's current
version sets all three fields on the `LeaveEntry` at creation time.

## Fix

Set `appliedBy`, `appliedByName`, and `appliedAt` when inserting a new leave row in
`POST /roster-plan/leave`, matching what the schema already expects. Cheap, self-contained fix —
the schema support already exists, this is purely a missing write.
