Type: bug
Status: done

> *This was generated by AI during triage. Small, easy win.*

## What happened

`Register.tsx` (the unauthenticated signup page) still calls the full `/api/roster-plan/officers`
endpoint. This repo's own backend **already has** a purpose-built
`/api/roster-plan/officer-names` endpoint — with a code comment noting it's mid-port from Replit
— that returns a minimal `{id, name, unitCode?, catchment?}` list instead of the full officer
record set to an unauthenticated caller.

## Fix

Switch `Register.tsx` to call `/api/roster-plan/officer-names` instead of the full `/officers`
endpoint. Also add optional-chaining for officers missing `unitCode`/`catchment` while in this
code, since those fields are optional on the minimal endpoint's response.

## Comments

- 2026-09-21: Fixed, with one adjustment found necessary along the way: `/officer-names` doesn't
  filter by `active` at all (its only other consumer, `RosterBuilder.tsx`, deliberately wants
  inactive/historical officer names too), while Register.tsx's original code explicitly filtered
  to active officers. Switching endpoints blind would have silently let deactivated officers
  appear as pickable on the signup page — a real regression. Added an opt-in `?activeOnly=1`
  query param to `/officer-names` (defaults to off, preserving RosterBuilder.tsx's existing
  behavior) and had `Register.tsx` pass it.
