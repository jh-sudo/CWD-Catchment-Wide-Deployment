Type: bug
Status: done

> *This was generated by AI during triage. Paired frontend (`manager.ts`'s inline JS) and backend
> (`deployments.ts`) change — port together.*

## What happened

**Frontend** (`manager.ts`'s `runRainAnalysis()`/`renderRainModal()`/`confirmRainAssign()`): the
current "Rain-Path Assign" button is always clickable regardless of data quality. `hasRain` is
computed from a blended location score (`current + 1.5*trend + 2*incoming`) that conflates "is
there rain" with "will a specific location be hit," and the average movement vector is always
trusted even when frame-to-frame vectors are wildly inconsistent (radar noise).

**Backend** (`deployments.ts`'s `rain-auto-assign`): fetches NEA station rainfall server-side and
assigns by crude region/cluster proximity, ignoring actual crew GPS.

Replit's rewrite separates `hasRain` (an actual radar blob, `centroids[0].w > 1`) from
`hasProjectedHits` (will it hit a scored location in 15-30 min), adds a `motionReliable` check
requiring at least 2 consistent movement vectors, and **disables** the assign button ("Movement
uncertain") when direction can't be trusted rather than letting a manager act on noise. The
matching backend rewrite takes client-projected `locationScores`/`projectedHitMinutes` and
greedily matches the *nearest* fresh-GPS crew (10-minute freshness gate), reporting
`skippedNoGps`.

## Fix

Port both halves together: the `hasRain`/`hasProjectedHits`/`motionReliable` split and button-gate
in `manager.ts`, and the nearest-fresh-GPS-crew matching algorithm in `deployments.ts`'s
`rain-auto-assign`, replacing the old NEA-station/cluster-proximity approach.

Related, smaller, optional: Replit's `optimize-assign` "nearest selected" mode can also reassign
already-deployed teams to a closer location (not just fill unassigned ones) — see `map.md` item
13 if picking this up too while in this code.

## Comments

- 2026-09-21: Fixed, with a better path than originally scoped once the full picture became
  clear. Discovered this repo's `/deployments/optimize-assign` endpoint (already built, its own
  default/"rain" mode) **already does** nearest-fresh-GPS-crew matching with proper rain scoring
  and Tier-1 gating — i.e., the correct algorithm this ticket asked for already existed. The bug
  was that `manager.ts`'s `confirmRainAssign()` was calling the older, cruder
  `/deployments/rain-auto-assign` instead (which didn't even read its own request body, despite
  the frontend already sending `locationScores`). So instead of rewriting the algorithm, switched
  the frontend to call the already-correct endpoint, and deleted `rain-auto-assign` entirely
  (confirmed via repo-wide grep it had no other callers) rather than leave two competing
  implementations around.
  - Also implemented the `hasRain`/`hasProjectedHits`/`motionReliable` split in
    `runRainAnalysis()`: `hasRain` is now "is there an actual radar blob right now"
    (`!!centroids[0]`, which `radarCentroid()` already gates at `w > 1`); `hasProjectedHits` is
    the old blended-score-based definition, now correctly used only to decide which assignment
    path to take. `motionReliable` requires ≥2 frame-to-frame vectors, a non-negligible average
    magnitude, and every individual vector agreeing in direction with the average (positive dot
    product) — when false, the assign button is disabled with a "Movement uncertain" state
    instead of letting a manager act on unreliable radar noise.
  - This whole section is inside one large template literal (the HTML `manager.ts` serves), so
    `tsc` doesn't validate any of this JS syntax — manually proofread the edits carefully since
    there's no compiler safety net here.
