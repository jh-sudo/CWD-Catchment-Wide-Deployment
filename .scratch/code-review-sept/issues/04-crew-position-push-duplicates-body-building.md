Type: quality
Status: done

> *This was generated by AI during triage (ultrareview, ~34-commit cloud
> review, 2026-09-08).*

Found during a cloud multi-agent review of `phase-b/medium-features`
requested to cover `b45d04b..HEAD` (34 commits), scoped in practice to the
working-tree diff against `8455c5a`. Falls inside the note's "crew.ts
continuous-tracking WIP" batch — `pushLastKnownPosition` is the new
continuous-watch push path added alongside the existing manual/accept-ping
`updateLocation`.

## Agent Brief

**Category:** quality (duplication risk between two position-push paths)
**Summary:** `pushLastKnownPosition` (new, continuous-watch) and
`updateLocation` (existing, manual/accept/refocus ping) build the same
`/api/deployments/position` request body and run the same ETA-attachment
logic almost verbatim — a future change to either has to be applied in both
places or the two paths silently diverge.

**Current behavior:**
`artifacts/api-server/src/routes/crew.ts:664-687` (`pushLastKnownPosition`)
and `crew.ts:692-719` (`updateLocation`) both:
1. Build `{vehicleId, vehicleNumber, unitCode, partner, shift, lat, lng, acceptedLocationId}` from `myTeam()`/`myEntry()` and a position.
2. Run the identical `if (entry && !entry.arrived) { destLoc = locationById(entry.locationId); est = estimateEta(...); body.eta = est.eta; body.etaMinutes = est.etaMinutes; }` block.
3. POST the body to `/api/deployments/position` (fire-and-forget in the
   continuous path, awaited-with-toast in the manual path — the only
   material difference besides the position source: cached `lastKnownPos`
   vs. a fresh `getCurrentPosition()` call).

Any future change to what a position ping needs to send (a new field the
manager map expects, a tightened ETA calc, or a fix to the entry-lookup
logic) has to be made in both functions or the continuous-watch pushes and
the manual-button push silently diverge in what they report.

**Desired behavior:**
Extract a shared `buildPositionBody(pos, t, entry)` (or similar) helper that
both callers use, so there's one place that defines what a position ping
contains.

**Key interfaces:**
- `pushLastKnownPosition()` — `artifacts/api-server/src/routes/crew.ts:664-687`
- `updateLocation()` — same file, `crew.ts:692-719`

**Acceptance criteria:**
- [ ] Body-building + ETA-attachment logic lives in one shared function
- [ ] Both `pushLastKnownPosition` and `updateLocation` call it; behavior
      unchanged (continuous path still fire-and-forget, manual path still
      awaited with toast)

## Comments

> *This was generated by AI during triage.*

Extracted `buildPositionBody(t, entry, lat, lng)`, containing the shared
body-assembly + ETA-attachment logic. Both `pushLastKnownPosition` (fire-
and-forget continuous push) and `updateLocation` (awaited, toasts on
completion) now call it and only differ in position source and how the
`fetch`/`postJson` result is handled, as before.

Verified: `pnpm --filter api-server typecheck` clean.
