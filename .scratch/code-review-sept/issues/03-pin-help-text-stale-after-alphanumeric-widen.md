Type: quality
Status: done

> *This was generated by AI during triage (ultrareview, ~34-commit cloud
> review, 2026-09-08).*

Found during a cloud multi-agent review of `phase-b/medium-features`
requested to cover `b45d04b..HEAD` (34 commits), scoped in practice to the
working-tree diff against `8455c5a`. Not part of the note's named batches
(roster QA / crash-loop / crew tracking) — appears to be leftover copy from
an earlier PIN-widening change bundled into this diff.

## Agent Brief

**Category:** quality (stale UI copy contradicts the validator it describes)
**Summary:** The Manager/Crew PIN settings panel still tells admins the PIN
is "4–8 digit" only, but the same diff widened both PIN validators (and
their error copy) to accept letters too — the help text now actively
undersells the feature.

**Current behavior:**
`artifacts/api-server/src/routes/manager.ts:914` and `:927`:
```html
<p ...>4–8 digit PIN required to access the Manager tab on the phone app.</p>
...
<p ...>4–8 digit PIN required for crew members to log in on the phone app.</p>
```
Elsewhere in the same file, the client-side `updatePin`/`updateCrewPin`
validators were widened from `/^\d{4,8}$/` to `/^[A-Za-z0-9]{4,8}$/`, with
their error strings updated to "PIN must be 4–8 letters/digits" — and
`crew.ts` had `inputmode="numeric"` removed from the crew PIN field for the
same reason. The two `<p>` help lines were missed, so an admin reading the
on-screen guidance is told the field is digit-only and won't discover the
alphanumeric capability this diff specifically added — undercutting the
crew-side keyboard fix.

**Desired behavior:**
Both help lines read "4–8 letters/digits" (or equivalent), matching the
validator and error copy.

**Key interfaces:**
- `artifacts/api-server/src/routes/manager.ts:914` (Manager PIN help text)
- `artifacts/api-server/src/routes/manager.ts:927` (Crew PIN help text)
- `updatePin`/`updateCrewPin` validators, same file (~line 1477, ~1501)

**Acceptance criteria:**
- [ ] Both `<p>` help strings say "4–8 letters/digits" (or match whatever
      final wording the validator error strings use)

## Comments

> *This was generated by AI during triage.*

Both `<p>` help lines updated to "4–8 letters/digits", word-for-word
matching the existing `updatePin`/`updateCrewPin` validator error copy
("PIN must be 4–8 letters/digits.") already in the same file.

Verified: `pnpm --filter api-server typecheck` clean.
