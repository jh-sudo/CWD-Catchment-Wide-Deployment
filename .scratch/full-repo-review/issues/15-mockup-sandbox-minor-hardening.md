Type: task
Status: done

> *This was generated by AI during triage.*

Found during a 9-way parallel code review of the full repo (2026-08-21), in
the `artifacts/mockup-sandbox` slice. `mockup-sandbox` is a confirmed inert
Replit design-canvas prototype ("Warehouse IMS") — no Dockerfile, no network
calls, not referenced by any other package, not part of the deploy path. Both
items below are low-severity precisely because of that; filed for
completeness rather than urgency.

## Agent Brief

**Category:** task (hardening, low priority)
**Summary:** Two minor anti-patterns in throwaway prototype code, worth a note in case the scaffolding gets copy-pasted into a real app later.

**Current behavior:**
1. `artifacts/mockup-sandbox/src/store.ts:81-89` and
   `src/lib/data.ts:128-132` — mock "login" does a plaintext password
   string-comparison against a hardcoded array of fake users (fabricated
   `.gov.sg` emails, plaintext passwords like `"Store@1234"`). Client-only
   Zustand store, not connected to any real auth system.
2. `artifacts/mockup-sandbox/vite.config.ts:34-46` — dev/preview server binds
   `host: "0.0.0.0"` with `allowedHosts: true`, disabling Vite's
   Host-header/DNS-rebinding protection. Standard Replit-preview boilerplate
   (needed for Replit's dynamic-hostname proxy), dev-server-only.

**Desired behavior:**
Neither is expected to be "fixed" in place — this ticket exists so the
pattern is documented if this login form or vite config ever gets reused as a
starting point for real code.

**Key interfaces:**
- `artifacts/mockup-sandbox/src/store.ts`, `src/lib/data.ts`, `vite.config.ts`

**Acceptance criteria:**
- [ ] If this scaffolding is ever promoted into a real, deployed app, the mock login is replaced with real auth and `allowedHosts`/`host` are reviewed for the target environment

**Out of scope:**
- Changing anything in `mockup-sandbox` itself right now — it's inert and not deployed

## Comments

> *This was generated by AI during triage.*

**Closed, no code change** — per this ticket's own Desired Behavior/Out of
Scope, it exists purely to document the pattern for whoever might reuse this
scaffolding later, not to fix `mockup-sandbox` now (confirmed still inert:
no Dockerfile, not referenced by any other package, not part of the deploy
path — same as when triaged). Re-verified both findings still describe the
current code (`store.ts`/`data.ts` plaintext mock login, `vite.config.ts`
`host: "0.0.0.0"` + `allowedHosts: true`) so the documentation this ticket
provides is still accurate and needs nothing further.
