Type: task
Status: done

> *This was generated by AI during triage.*

Found during a 9-way parallel code review of the full repo (2026-08-21), in
the `docs/` slice. Two small, independent doc corrections bundled together.

## Agent Brief

**Category:** docs
**Summary:** `CONTEXT.md` states a product decision is "still an open question" when it was resolved in the same commit that wrote that sentence, and `docs/agents/issue-tracker.md` links to a `triage-labels.md` file that doesn't exist.

**Current behavior:**
1. `CONTEXT.md:63` reads: "Deciding whether officer deletion should ever
   cascade or reassign related records instead of blocking/deactivating is
   still an open question (see
   `.scratch/postgres-migration-fixes/issues/05-officer-delete-fk-restrict.md`)."
   The cited ticket's `Status:` is `done`, its acceptance criteria show
   `[x] (Resolved, see Comments) ... Decision: soft-delete (deactivate)`, and
   the one remaining gap is explicitly labeled "accepted as out of scope," not
   open. `git show d54e303` confirms the preceding "Officer deletion is
   soft-delete" section and the ticket's resolution to `done` were added in
   the *same commit* — this sentence was stale from the moment it was
   written, contradicting the section it's attached to. This has already
   propagated into at least one derived source (an AI assistant's persisted
   memory of this repo described the decision as "still open" based on this
   sentence).
2. `docs/agents/issue-tracker.md:10` reads: "...(see `triage-labels.md` for
   the role strings)". No file named `triage-labels.md` exists anywhere in
   the repo. The doc's own "Wayfinding operations" section (line 26) states
   ticket status is `claimed`/`resolved`, while real ticket files observed in
   `.scratch/` use `not-started`/`in-progress`/`done`/`resolved` — suggesting
   the promised status vocabulary was never written down anywhere, or was
   deleted.

**Desired behavior:**
1. `CONTEXT.md:63`'s final sentence is rewritten or removed — the decision is
   made; only the accepted-as-out-of-scope hard-delete gap remains, and it can
   be stated plainly without "open question" framing.
2. `docs/agents/issue-tracker.md` either gets a real `triage-labels.md`
   enumerating the actual status vocabulary in use, or the parenthetical is
   struck and the valid values are inlined directly in `issue-tracker.md`.

**Key interfaces:**
- `CONTEXT.md`
- `docs/agents/issue-tracker.md`

**Acceptance criteria:**
- [ ] `CONTEXT.md` no longer describes the officer-delete cascade decision as open
- [ ] `docs/agents/issue-tracker.md` no longer links to a nonexistent file, and states the actual `Status:` vocabulary somewhere

**Out of scope:**
- Minor terminology mismatch also spotted in the same pass: `issue-tracker.md:25` calls the map's third section "Fog," but real map files (e.g. `.scratch/govpaas-migration/map.md`) head it `## Not yet specified` (an HTML comment there does use "fog" informally). Worth a one-word alignment whenever this file is next touched, not urgent enough for its own acceptance criterion.

## Comments

> *This was generated by AI during triage.*

**Fixed, both items, plus the noted out-of-scope one-word alignment.**

1. `CONTEXT.md:63`'s final sentence rewritten — no longer frames the
   officer-delete cascade decision as open; states plainly that soft-delete
   was the decision and the zero-history hard-delete gap is accepted as out
   of scope (matching the resolved ticket's actual Comments/Acceptance
   criteria wording).
2. `docs/agents/issue-tracker.md:10`'s dangling `triage-labels.md` reference
   struck; the actual status vocabulary (`not-started`/`in-progress`/`done`/
   `resolved`, freeform detail allowed after a dash) is now inlined directly,
   with a note that wayfinding tickets intentionally use their own narrower
   `claimed`/`resolved` vocabulary — grepped every `Status:` line under
   `.scratch/` to confirm this is the real, currently-used set rather than
   guessing.
3. Also applied the one-word "Fog" → "Not yet specified" alignment noted as
   out-of-scope-but-worth-doing-when-next-touched, since this file was
   already being touched — `map.md` files head that section "Not yet
   specified", not "Fog" (an HTML comment there uses "fog" informally, which
   is fine to leave as-is).
