Type: task
Status: done

> *This was generated by AI during triage.*

Found during a 9-way parallel code review of the full repo (2026-08-21), in
the `artifacts/apa` and `artifacts/inspector` slices. Both apps were
generated from the same Replit/shadcn template and never had the unused parts
trimmed. Doesn't affect the shipped bundle (Vite doesn't bundle unimported
code) — this is repo-clutter/maintenance-surface cleanup, not a bug.

## Agent Brief

**Category:** task (simplification)
**Summary:** ~120 files of unimported shadcn/ui scaffold and unused dependencies across the two smallest frontend apps.

**Current behavior:**
- `artifacts/inspector/src/components/ui/*` (60+ files), `src/hooks/use-toast.ts`,
  `src/hooks/use-mobile.tsx`, `src/lib/utils.ts`, `src/pages/not-found.tsx` —
  confirmed unreachable from the app's only entry point
  (`main.tsx` → `App.tsx` → `ManagerDashboard.tsx`), along with unused
  `package.json` devDependencies (`wouter`, `@tanstack/react-query`,
  `react-hook-form`, `zod`, `recharts`, `embla-carousel-react`, `vaul`,
  `cmdk`, etc.).
- `artifacts/apa/src/components/ui/*` (~55 files), `src/hooks/use-toast.ts`,
  `src/hooks/use-mobile.tsx`, `src/pages/not-found.tsx` — same pattern,
  confirmed unimported from `App.tsx`/`main.tsx`, plus unused deps (`wouter`,
  `react-hook-form`, `@hookform/resolvers`, `@tanstack/react-query`,
  `@workspace/api-client-react`, `recharts`, `embla-carousel-react`, `cmdk`,
  `vaul`, `framer-motion`, `next-themes`, `input-otp`, etc.).

**Desired behavior:**
Both packages carry only the code and dependencies they actually use.

**Key interfaces:**
- `artifacts/inspector/src/{components/ui,hooks,lib,pages}`, `package.json`
- `artifacts/apa/src/{components/ui,hooks,pages}`, `package.json`

**Acceptance criteria:**
- [ ] `artifacts/inspector`: unused scaffold directories/files removed, `package.json` pruned of the now-unused devDependencies, app still builds and runs
- [ ] `artifacts/apa`: same

**Out of scope:**
- Any scaffold file that's actually imported somewhere (re-verify with a fresh grep before deleting, in case this changes after `02`/`07`/`08` land)

## Comments

> *This was generated by AI during triage.*

**Fixed, both apps.** Re-verified reachability with a fresh repo-wide grep
before deleting anything (per this ticket's own Out of Scope note), rather
than trusting the original triage list as-is:

- Confirmed `components/ui/*`, `hooks/use-toast.ts`, `hooks/use-mobile.tsx`,
  `lib/utils.ts`, and `pages/not-found.tsx` are unreachable from each app's
  real entry point (`main.tsx` → `App.tsx`[ → `ManagerDashboard.tsx` for
  inspector]) in both apps — only cross-referenced among themselves.
- **`apa/src/lib/utils.ts` was not in the original triage list** (only
  inspector's was) but the same grep shows it's *also* only referenced from
  `components/ui/*` in `apa` — same dead pattern, just missed the first time.
  Deleted it too.
- Systematically checked every devDependency against each app's reachable
  file set (not just the "etc."-truncated list in the brief) — found two
  more unused ones beyond what was named: **`lucide-react`** (both apps) and
  **`@workspace/api-client-react`** (`inspector` — apa's copy was already
  named). Pruned those too.
- Left `tailwindcss`, `@tailwindcss/vite`, `@tailwindcss/typography`,
  `tw-animate-css` alone even though nothing in the real app imports them as
  JS — they're wired into the live CSS pipeline (`index.css`'s `@import`/
  `@plugin` directives, `vite.config.ts`'s plugin list), a different kind of
  "in use" than a JS import, and out of this ticket's scope to touch.

Deleted 61 files from `inspector`, 56 from `apa`. Pruned 28 devDependencies
from each `package.json` (`@radix-ui/*` × 27, `@hookform/resolvers`,
`@tanstack/react-query`, `class-variance-authority`, `clsx`, `cmdk`,
`date-fns`, `embla-carousel-react`, `framer-motion`, `input-otp`,
`lucide-react`, `next-themes`, `react-day-picker`, `react-hook-form`,
`react-icons`, `react-resizable-panels`, `recharts`, `sonner`,
`tailwind-merge`, `vaul`, `wouter`, `zod`, `@workspace/api-client-react`).

**Verified**: `pnpm install` (lockfile updated), `pnpm run typecheck` clean
for both apps, `pnpm run build` succeeds for both (inspector: 77 modules
transformed, clean production bundle).
