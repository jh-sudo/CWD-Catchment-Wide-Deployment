Type: task
Status: done

> *This was generated by AI during triage.*

Found during a 9-way parallel code review of the full repo (2026-08-21), in
the `scripts/` slice. Cosmetic — doesn't affect what data gets written,
only what `--dry-run` reports.

## Agent Brief

**Category:** task (reporting accuracy)
**Summary:** One backfill warning is only computed in live-run mode, so `--dry-run` doesn't preview it.

**Current behavior:**
`scripts/src/backfill/02-core-ops-state.ts:337` — the warning
`"deployment_settings.deployment_date has no source value ... defaulted to
today"` is only pushed inside the `if (!dryRun)` branch (lines 268-337).
Every other warning in this script (and the other backfill scripts) is
computed independent of `dryRun` and simply not *acted on* in dry-run mode —
this one is the exception, so a `--dry-run` invocation doesn't accurately
preview what the live run will report.

**Desired behavior:**
The warning is computed (and printed) the same way in both dry-run and live
mode, matching every other warning in this script.

**Key interfaces:**
- `scripts/src/backfill/02-core-ops-state.ts:268-337`

**Acceptance criteria:**
- [ ] `--dry-run` output includes this warning whenever the live run would produce it

**Out of scope:**
- Any actual data-writing behavior — unaffected by this ticket

## Comments

> *This was generated by AI during triage.*

**Fixed.** Moved the `warnings.push(...)` call out of the `if (!dryRun) {
... }` block, so it's computed unconditionally (still gated on `if (state)`,
same as before — nothing to warn about if `state.json` doesn't exist) —
matching every other warning in this script (e.g. the `managers.json has
officer-linked accounts` warning a few lines up, which was already outside
its own `if (!dryRun && ...)` write-guard).

**Verified live**: ran `pnpm run backfill -- --dry-run` against local
Postgres. The warning now appears in dry-run output —
`⚠  deployment_settings.deployment_date has no source value in state.json —
defaulted to today (2026-08-25)` — alongside the other 2 pre-existing
warnings, none of which changed. No writes occurred (dry-run contract
unaffected).
