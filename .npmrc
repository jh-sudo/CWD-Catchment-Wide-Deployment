auto-install-peers=false
strict-peer-dependencies=false
# Same protection as pnpm-workspace.yaml's minimumReleaseAge, for the npm
# config surface too — a newly published package version can be malicious
# or unstable before the ecosystem has had a chance to flag it.
min-release-age=7
